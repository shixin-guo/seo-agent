[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
dspy-ai = ">=2.0.0"
python-dotenv = ">=1.0.0"
pyyaml = ">=6.0"
click = ">=8.1.0"
tqdm = ">=4.66.0"
requests = ">=2.30.0"
beautifulsoup4 = ">=4.12.0"
pandas = ">=2.0.0"

[dev-packages]
ruff = ">=0.1.6"
mypy = ">=1.3.0"
pre-commit = ">=3.3.2"
types-requests = ">=2.31.0"
types-PyYAML = ">=6.0.12"
pytest = ">=7.0.0"
pytest-cov = ">=4.0.0"
pytest-mock = ">=3.10.0"

[requires]
python_version = "3.11"

[scripts]
lint = "ruff check seo_agent main.py --extend-exclude tests/"
format = "ruff format seo_agent main.py"
typecheck = "mypy seo_agent main.py"
test = "pytest"
test-unit = "pytest -m unit"
test-integration = "pytest -m integration"
test-cov = "pytest --cov=seo_agent --cov-report=term"
keyword-research = "python main.py keyword-research"
content-optimize = "python main.py optimize-content"
site-audit = "python main.py audit-site"
backlink-research = "python main.py backlink-research"
mock = "python tests/mock/simple_keyword_demo.py --seed \"digital marketing\" --industry \"saas\" --auto-csv"
